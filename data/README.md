# Data

Raw and materialized data in this directory are ignored. For each source,
record its identifier/URL, revision, license/access terms, checksum, schema,
transformation command, split lineage, and durable storage location here or
in a linked manifest. Keep sensitive inputs out of tracked metadata.
